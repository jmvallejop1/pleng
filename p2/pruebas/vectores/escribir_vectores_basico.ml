programa escribir_vectores_basico;

entero   e[3];
booleano b[3];
caracter c[3];

Principio
	escribir(b,e);
	
	%% Funciona correctamente en la generacion de codigo
	escribir(b);
	escribir(e);
	escribir(e[3]);
	escribir(b[1]);
	escribir(c[1]);%%
Fin